import Vue from 'vue'

import {
  Input,
  Upload,
  Icon,
  Dialog,
  Select,
  Option,
} from 'element-ui'

Vue
  .use(Input)
  .use(Upload)
  .use(Icon)
  .use(Dialog)
  .use(Select)
  .use(Option)